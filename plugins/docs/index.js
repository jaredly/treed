
var React = require('react')
var cx = require('classnames')

module.exports = {
  types: {
    table: {
      shortcut: '#',
    },
  },

  // any keys, etc needed?

  node: {
    bodies: {
      table: {
        renderer: function () {
        },

        editor: function (props) {
        },
      },
    },
  },

}

